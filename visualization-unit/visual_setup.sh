this file needs to:
  install python on the raspberry pi
  install relevant libraries for the function of MUAIR
  organize folders in the same manner as the original raspberry pis
  copy python files into correct folders
  copy .sh files into correct folders
  set up crontasks for each .sh file in the same manner as the original raspberry pi
